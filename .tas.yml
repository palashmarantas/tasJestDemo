---
framework: jest
licenseTier: small
postMerge: 
  pattern: 
    - "./src/*.test.js"
preMerge:
  pattern:
    - "./src/*.test.js"
preRun:
     command:
        - npm install
postRun:
     command:
        - node --version
version: 2.1
